[project]
name = "branding"
authors = [
    { name = "Sibyl Technologies", email = "hello@tijaratapp.com" }
]
description = "No-code branding: labels, menus, and theme colors for Tijarat - independent of any specific ERPNext redesign."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
